import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ArchLens - AWS Architecture Analysis',
  description: 'AI-powered AWS architecture analysis for security, performance, and best practices',
  keywords: ['AWS', 'architecture', 'analysis', 'security', 'well-architected'],
  authors: [{ name: 'ArchLens Team' }],
  viewport: 'width=device-width, initial-scale=1',
  robots: 'index, follow',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}